# Web

Next.js app for the monorepo.

## Development
- From the repo root: `bun run dev` (runs via Turborepo on port 3000).
- From this folder: `bun dev`.

## Production
- `bun run build` to build.
- `bun run start` to serve the built app.
